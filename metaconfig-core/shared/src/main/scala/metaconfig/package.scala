package object metaconfig {
//  implicit class XtensionEither[T](res: Configured[T]) {
//    def get: T = res match {
//      case Configured.NotOk(e) => throw new IllegalStateException(e.msg)
//      case Configured.Ok(x) => x
//    }
//  }
//  type Configured[T] = Either[ConfError, T]
}
